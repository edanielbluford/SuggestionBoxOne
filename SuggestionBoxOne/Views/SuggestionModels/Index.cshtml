@model IEnumerable<SuggestionBoxOne.Models.SuggestionModel>

@{
    ViewBag.Title = "Index";
}

<h2>Please Leave a Suggestion</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SuggestionName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SuggestionComment)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SuggestionName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SuggestionComment)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RecordNum }) |
            @Html.ActionLink("Details", "Details", new { id=item.RecordNum }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RecordNum })
        </td>
    </tr>
}

</table>
